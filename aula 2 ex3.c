#include <stdio.h>

int main() {
    
    float bct = 479609.00;
    float euro = 6.41; 
    float usd = 5.86;
    
    printf("Cotação do dolar (usd): %.2f\n", usd);
    printf("Cotação do euro (euro): %.2f\n", euro);
    printf("Cotação do bitcoin (btc): %.2f\n", bct);
  
  return 0;
} 